# RTL v2

RTL v2 is the current factor-reuse optimization baseline migrated from
`CSCGRA_opt_architecture_opt2`.

The layout migration does not change RTL behavior. In particular, it preserves
the existing algorithm schedule, factor reuse, PE routing, PE0 input rule, and
four-row PE organization.

Use `files.f` for compile order.
