# RTL v1 verification

This directory preserves testbenches and compiled golden vectors matched to
the frozen `rtl/v1` snapshot. The `run1/` suite is the v1 historical regression
source selected by `config/rtl-v1.json`.

Do not mix these vectors or file lists with `rtl/v2`. Current optimization
sign-off uses `verification/v2`, not this directory.
