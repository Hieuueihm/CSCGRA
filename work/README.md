# Tool workspaces

Vivado, XSim, Verilator, SDK/Vitis, and analysis tools create disposable state
below:

`work/<flow>/<rtl-version>/<run-id>/`

Only this README is tracked. It must be safe to remove any individual run
directory after its required summary has been reviewed.
