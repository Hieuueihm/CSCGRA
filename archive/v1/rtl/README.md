# RTL v1

RTL v1 is the preserved `CSCGRA_opt_architecture` snapshot from the
pre-layout-refactor repository. It is intentionally independent from v2.

Use `files.f` for compile order. Do not compile files from old generated
`CSCGRA.srcs` directories.
